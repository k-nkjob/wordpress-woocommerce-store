(function(){'use strict';function firstVisible(card,selectors){for(var i=0;i<selectors.length;i++){var el=card.querySelector(selectors[i]);if(el&&el.offsetParent!==null)return el;}return null;}function columnCount(grid,cards){var style=window.getComputedStyle(grid);if(style.display.indexOf('grid')!==-1&&style.gridTemplateColumns&&style.gridTemplateColumns!=='none'){var count=style.gridTemplateColumns.trim().split(/\s+/).filter(Boolean).length;if(count>0)return count;}if(cards.length<2)return 1;var top=Math.round(cards[0].getBoundingClientRect().top);var sameRow=0;for(var i=0;i<cards.length;i++){if(Math.abs(Math.round(cards[i].getBoundingClientRect().top)-top)<=10)sameRow++;else break;}return Math.max(1,sameRow);}function equalize(grid,config){if(!grid||!config)return;var cards=Array.prototype.slice.call(grid.querySelectorAll(':scope > '+config.cardSelector)).filter(function(card){return card.offsetParent!==null;});if(!cards.length)return;var fields=config.fields||{};Object.keys(fields).forEach(function(key){cards.forEach(function(card){var el=firstVisible(card,fields[key]);if(!el)return;el.style.removeProperty('height');el.style.removeProperty('min-height');});});void grid.offsetWidth;var columns=columnCount(grid,cards);for(var i=0;i<cards.length;i+=columns){var row=cards.slice(i,i+columns);Object.keys(fields).forEach(function(key){var els=row.map(function(card){return firstVisible(card,fields[key]);}).filter(Boolean);if(els.length<2)return;var max=0;els.forEach(function(el){max=Math.max(max,Math.ceil(el.getBoundingClientRect().height));});if(max<=0)return;els.forEach(function(el){el.style.setProperty('height',max+'px','important');el.style.setProperty('min-height',max+'px','important');});});}}window.KNKRowEqualizer={equalize:equalize};})();
