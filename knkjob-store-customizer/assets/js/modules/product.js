(function($){'use strict';function updateLowStock(variation){var $message=$('[data-knk-low-stock]');if(!$message.length)return;var qty=parseInt(variation&&variation.max_qty,10);if(Number.isFinite(qty)&&qty>0&&qty<=5){$message.text('残り '+qty+' 点').prop('hidden',false);}else{$message.text('').prop('hidden',true);}}$(document).on('found_variation','.variations_form',function(event,variation){updateLowStock(variation);}).on('reset_data hide_variation','.variations_form',function(){$('[data-knk-low-stock]').text('').prop('hidden',true);});})(jQuery);
